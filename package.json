{
  "name": "2024-annual-report",
  "version": "0.1.0",
  "license": "CC0-1.0",
  "author": "",
  "scripts": {
    "start": "open index.html && tailwindcss --watch --input styles.css --output output.css",
    "macos:watch:chrome": "fswatch -o index.html output.css | xargs -n1 -I {} osascript -e 'tell application \"Google Chrome\" to tell the active tab of its first window to reload'",
    "build": "tailwindcss --minify --input styles.css --output output.css"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.17",
    "prettier": "^3.5.3",
    "tailwindcss": "^4.0.17"
  }
}
